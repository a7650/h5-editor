import { ImageWidget, TextWidget, BackgroundWidget, RectWidget } from './index'

export default {
    image: ImageWidget,
    text: TextWidget,
    background: BackgroundWidget,
    rect: RectWidget
}
